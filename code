import pygame

pygame.init()
screen = pygame.display.set_mode((400, 300))
done = False

while done == False:
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            done = True
